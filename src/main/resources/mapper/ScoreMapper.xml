<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.neusoft.springbookdemo.mapper.ScoreMapper">

    <insert id="insert" parameterType="ScoreRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO score_record(user_id, score, source, remark)
        VALUES(#{userId}, #{score}, #{source}, #{remark})
    </insert>

    <select id="findByUser" resultType="ScoreRecord">
        SELECT * FROM score_record WHERE user_id = #{userId} ORDER BY id DESC
    </select>
</mapper>
