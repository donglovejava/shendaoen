<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.neusoft.springbookdemo.mapper.UserMapper">

    <select id="findByUsername" resultType="User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultType="User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, nickname, role)
        VALUES(#{username}, #{password}, #{nickname}, #{role})
    </insert>

    <update id="addScore">
        UPDATE user SET total_score = total_score + #{delta} WHERE id = #{userId}
    </update>

    <select id="topByScore" resultType="User">
        SELECT * FROM user WHERE role = 'STUDENT' ORDER BY total_score DESC LIMIT #{limit}
    </select>
</mapper>
