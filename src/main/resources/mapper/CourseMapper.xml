<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.neusoft.springbookdemo.mapper.CourseMapper">

    <select id="findAll" resultType="Course">
        SELECT * FROM course ORDER BY id DESC
    </select>

    <select id="findById" resultType="Course">
        SELECT * FROM course WHERE id = #{id}
    </select>

    <select id="findByTeacher" resultType="Course">
        SELECT * FROM course WHERE teacher_id = #{teacherId} ORDER BY id DESC
    </select>

    <insert id="insert" parameterType="Course" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO course(title, description, cover, teacher_id)
        VALUES(#{title}, #{description}, #{cover}, #{teacherId})
    </insert>

    <update id="update" parameterType="Course">
        UPDATE course SET title = #{title}, description = #{description}, cover = #{cover}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM course WHERE id = #{id}
    </delete>
</mapper>
